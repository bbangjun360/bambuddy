# Purpose

Implement WP-010: using the unchanged Bambuddy UI, an operator can upload the approved fixture STL, request server-side Orca slicing through Bambuddy, and see the generated `.gcode.3mf` in the Bambuddy Library.

# Current Behavior

- Baseline observed on 2026-06-24T12:12:20+09:00 from branch `feature/wp-010-orca`.
- `make verify-fast` passed before edits. Evidence: context budget, workpack check, hook py_compile, harness unittest contract/unit/characterization all passed.
- `backend/app/api/routes/library.py` already exposes:
  - `POST /api/v1/library/files` to upload STL/3MF/gcode files into `LibraryFile`.
  - `POST /api/v1/library/files/{file_id}/slice` to enqueue a background slice job.
  - `slice_and_persist()` to force `.gcode.3mf` output and store a new `LibraryFile` with `source_type="sliced"`.
  - `GET /api/v1/library/files/{file_id}` to return the Library row and metadata.
- `backend/app/services/slicer_api.py` already talks to an HTTP slicer sidecar with `GET /health`, `GET /profiles/bundled`, and multipart `POST /slice`.
- `backend/app/services/preset_resolver.py` already resolves `local`, `cloud`, `orca_cloud`, and `standard` preset refs. WP-010 will use local deterministic profiles in tests, not cloud credentials.
- `backend/app/schemas/settings.py` keeps `use_slicer_api` default `False`, preserving the generic production default.
- `backend/tests/integration/test_library_slice_api.py` now has WP-010 mocked sidecar coverage for valid STL, invalid model, invalid profile, timeout, provenance hashes, no queue dispatch, no credential forwarding, and temp cleanup.
- `harness/docker-compose.harness.yml` now has a `slicer` profile `orca-slicer-api` service with pinned image, healthcheck, localhost-only port, explicit DNS aliases, and no Bambu credential environment.
- Real sidecar health and slicing now pass with `ghcr.io/bbangjun360/orca-slicer-api:headless-cli-qualified-20260624@sha256:1c3e3e6f2ff193d4202bc7284af5d5a8c868e6185a26eeeacf878fe51387f518`. The harness uses the valid STL fixture plus P1P printer/process/filament profile JSON copied from that qualified image's bundled BBL profiles. The earlier maziggy image-family SIGSEGV diagnosis is retained below as historical evidence.

# Scope

In scope:

- WP-010 ExecPlan and evidence updates.
- Harness fixtures for valid STL, invalid model, invalid profile, timeout, and approved deterministic profiles.
- Tests for direct Orca sidecar contract shape and Bambuddy Library slice integration.
- Hash provenance for source model, output artifact, and profile set.
- Temporary-file cleanup assertion.
- Orca sidecar health/version check path.
- Default-off behavior preservation for `use_slicer_api`.

Out of scope:

- ERP, Obico, PrintFlow, bed automation, scheduler, costing, Bambu Studio CLI, STEP support, UI customization, printer dispatch, real printer connectivity, production credentials, and protected branch changes.

# Architecture Boundaries

- Bambuddy remains the only authority for printer state-changing commands.
- OrcaSlicer only receives model bytes and approved profile JSON; it must not receive Bambu printer credentials.
- The WP-010 slice result lands in Bambuddy Library only. No queue item and no printer dispatch is created.
- New behavior is additive and server-side; the existing Bambuddy UI controls remain unchanged.
- The slicer API path remains off in generic production defaults via `use_slicer_api=False`.

# Milestones

- [x] Baseline: clean branch and `make verify-fast` pass.
- [x] Harness fixtures and deterministic profile set added.
- [x] Failing WP-010 tests added for valid slice, invalid model, invalid profile, timeout, hashes, no dispatch, no credentials, and temp cleanup.
- [x] Product/server changes added for hash provenance and safe Library persistence under the mocked sidecar contract.
- [x] Harness sidecar health command added and run.
- [x] WP-000 and WP-010 validations pass with the qualified forked Orca sidecar image; `make harness-orca-slice` produces a real `.gcode.3mf` artifact.
- [x] Final diff reviewed for scope creep, secrets, generated artifacts, and rollback.
- [ ] Commit, push, and PR against `farm-main` after all Done When conditions are satisfied. Not allowed yet because real sidecar artifact creation is blocked.

# Progress

- [x] 2026-06-24T12:12:20+09:00 Read required root instructions, charter, WP-010, ORCA module, harness architecture, and ExecPlan format.
- [x] 2026-06-24T12:12:20+09:00 Verified clean working tree on `feature/wp-010-orca`.
- [x] 2026-06-24T12:12:20+09:00 Ran `make verify-fast`; passed.
- [x] 2026-06-24T12:12:20+09:00 Inspected existing Library upload, slice dispatch, slicer API client, preset resolver, settings default, and harness compose behavior.
- [x] Added harness fixtures/tests before product changes.
- [x] Pinned real Orca sidecar image `ghcr.io/maziggy/orca-slicer-api:bambuddy-0.2.4.7@sha256:c145e03b121d201dd0469b31758948e1932d2e6b4d8ddad92fe142a005919c80` and added `ORCA_API_PORT=13003`.
- [x] Added deterministic WP-010 fixture manifest with source/profile/profile-set hashes. The valid STL fixture is derived from the pinned Orca image's bundled `OrcaCube_v2.3mf`; invalid model/profile fixtures are local deterministic rejects.
- [x] Added `harness/tests/test_orca_wp010_contract.py` for fixture determinism, immutable image pin, explicit harness DNS aliases, sidecar healthcheck, localhost bind, dedicated Orca data volume, and absence of Bambu credential fields.
- [x] Added `harness/scripts/orca_health.py` and `harness/scripts/orca_direct_slice.py` plus Make targets `harness-orca-health` and `harness-orca-slice`.
- [x] Added WP-010 mocked integration coverage in `backend/tests/integration/test_library_slice_api.py`.
- [x] Updated `backend/app/api/routes/library.py` to record source/output/profile/profile-set hashes in Library and archive slicer provenance.
- [x] `make harness-health` passed after fixing explicit Compose DNS aliases for `postgres`, `mock-services`, `bambuddy`, and `orca-slicer-api`.
- [x] `make harness-orca-health` passed with OrcaSlicer version `2.3.2` and dataPath accessible.
- [x] `python3 -m unittest harness.tests.test_orca_wp010_contract` passed.
- [x] `make verify-fast`, `make test-unit`, and `make test-contract` passed before later fixture/profile updates; rerun required after final edits.
- [x] Direct real sidecar slice is blocked. Evidence: `make harness-orca-slice` returns HTTP 500 with `Slicer process failed (signal SIGSEGV)` for the required uploaded printer+process profile path.
- [x] Diagnostic matrix: no uploaded profiles fails safely with Orca validation error about `layer_gcode`; single uploaded profiles fail safely; uploaded `printerProfile+presetProfile` and full `printerProfile+presetProfile+filamentProfile` crash with SIGSEGV.
- [x] Direct container CLI reproduction: `/app/squashfs-root/AppRun --export-3mf result.3mf --slice 1 --load-settings /tmp/wp010-printer.json\;/tmp/wp010-process.json --load-filaments /tmp/wp010-filament.json --allow-newer-file --outputdir /tmp/wp010-out /tmp/wp010-fixture.stl` exits `139` with `Segmentation fault (core dumped)`.
- [x] Tested all available Bambuddy sidecar tags. `bambuddy-0.2.4.5@sha256:ef04e1bca525be45aea0f45910e44bb3aba0f003de8c695585737f05c5ec1c88`, `bambuddy-0.2.4.6@sha256:a908cede4c7d46b7ae5268711d24b07aa3894d6378cfe95d745aeea7a14f3809`, and `bambuddy-0.2.4.7@sha256:c145e03b121d201dd0469b31758948e1932d2e6b4d8ddad92fe142a005919c80` all reproduce the same SIGSEGV.
- [x] 2026-06-24T14:51:14+09:00 Reproduced the new qualified-image failure: `make harness-orca-slice` returned HTTP 500 with Orca parameter validation instead of SIGSEGV.
- [x] 2026-06-24T14:51:14+09:00 Confirmed root cause as stale profile fixtures: direct CLI and HTTP `/slice` both passed with the same STL and P1P profiles bundled inside `bbangjun360/orca-slicer-api:headless-cli-qualified-20260624`.
- [x] 2026-06-24T14:51:14+09:00 Replaced the WP-010 P1P printer/process/filament profile fixtures with the qualified image's bundled BBL profile JSON and updated `profile-set.json` hashes.
- [x] 2026-06-24T14:51:14+09:00 Updated the Orca image contract test to accept only digest-pinned GHCR images from approved repositories, reject `latest`, reject unpinned tags, and assert `.env.harness.example` documents the qualified forked image.

# Decisions

- Treat the existing WP-010 workpack as the approved design for the brainstorming gate. The user explicitly requested execution of WP-010 only and supplied concrete constraints and done-when behavior.
- Prefer deterministic local `LocalPreset` rows in integration tests. This avoids cloud credentials and keeps Orca credential-free.
- Add provenance metadata to the sliced `LibraryFile.file_metadata` rather than adding a migration for WP-010. The model already has a JSON metadata field and WP-010 does not require a schema migration.
- Keep UI untouched. Existing frontend already shows server-side slicing only when `use_slicer_api` is enabled.

# Harness Changes

Implemented:

- Added fixture files under `harness/fixtures/orca/`:
  - `fixture-cube-v1.stl`, deterministic STL derived from the pinned Orca image's bundled `OrcaCube_v2.3mf`,
  - `invalid-model-v1.stl`, deterministic invalid model,
  - `profiles/p1p-printer.json`, `profiles/p1p-pla-process.json`, and `profiles/p1p-pla-filament.json`, copied from the pinned sidecar's bundled BBL profiles,
  - `profiles/invalid-profile.json`, deterministic invalid profile.
- Added `harness/fixtures/orca/profile-set.json` with source, profile, and profile-set SHA-256 values.
- Added harness-side tests for fixture determinism, immutable sidecar pinning, compose DNS aliases, healthcheck, localhost-only sidecar port, dedicated data volume, no host/printer VLAN networking, and no Bambu credential fields.
- Added mocked Bambuddy slice integration coverage for valid output, invalid model, invalid profile, timeout, no queue dispatch, no credential forwarding, hash provenance, and temp cleanup.
- Added `harness-orca-health` and `harness-orca-slice` targets.

# Implementation

Changed files in scope:

- `Makefile`: added narrow Orca harness targets.
- `.env.harness`, `.env.harness.example`: added pinned Orca image/port configuration.
- `harness/docker-compose.harness.yml`: added real `orca-slicer-api` configuration, healthcheck, explicit DNS aliases, localhost bind, and data volume.
- `harness/fixtures/orca/**`: added deterministic valid/invalid model and profile fixtures plus manifest hashes.
- `harness/scripts/orca_health.py`: added sidecar health probe.
- `harness/scripts/orca_direct_slice.py`: added direct sidecar fixture slice probe and ignored evidence artifact output.
- `harness/tests/test_orca_wp010_contract.py`: added deterministic harness contract checks.
- `backend/tests/integration/test_library_slice_api.py`: added WP-010 mocked Library slice behavior tests.
- `backend/app/api/routes/library.py`: added slicer provenance hashing for Library and archive slice outputs.

No UI files, ERP, Obico, PrintFlow, scheduler, costing, bed automation, printer dispatch, production credentials, or core protected branch changes were made.

# Validation

Required commands:

- `make verify-fast`
- Targeted backend WP-010 tests, exact command to be recorded after tests are added.
- `make test-unit`
- `make test-contract`
- `make test-integration`
- `make verify-full` if lower-level gates pass and the harness can start.

Observable end-to-end scenario:

1. Upload `harness/fixtures/orca/fixture-cube-v1.stl` through `POST /api/v1/library/files`.
2. Slice through `POST /api/v1/library/files/{id}/slice` with deterministic approved profiles and preferred slicer `orcaslicer`.
3. Poll `GET /api/v1/slice-jobs/{job_id}` to completion.
4. Fetch `GET /api/v1/library/files/{result.library_file_id}` and assert a visible `.gcode.3mf` Library row with source/output/profile hashes.
5. Assert no `PrintQueueItem` exists.


# Validation Results

Latest validation after implementation:

- `python3 -m py_compile backend/app/api/routes/library.py backend/app/services/slicer_api.py backend/tests/integration/test_library_slice_api.py harness/scripts/orca_health.py harness/scripts/orca_direct_slice.py harness/tests/test_orca_wp010_contract.py`: passed.
- `python3 -m unittest harness.tests.test_orca_wp010_contract`: passed, 4 tests.
- `make harness-orca-health`: passed; sidecar reports healthy, OrcaSlicer `2.3.2`, dataPath accessible.
- `make test-contract`: passed, 11 tests.
- `make test-unit`: passed, 11 tests plus core py_compile.
- `make verify-fast`: passed; WP-000 deterministic gate remains green.
- `make test-integration`: passed; Bambuddy root, `/health`, `/docs`, and mock services all return 200.
- `make verify-full`: failed after passing fast gate and smoke because `python3 -m unittest discover -s harness/tests -p 'scenario_*.py'` found zero tests and returned unittest exit code 5. This is an existing WP-000 scenario-target gap; WP-010 scope forbids implementing WP-000 here.
- `make harness-orca-slice`: failed with HTTP 500 from the real sidecar: `Slicer process failed (signal SIGSEGV)`.

# Failure and Recovery

- Invalid model: sidecar failure is surfaced as a failed slice job; no Library output and no queue item are created.
- Invalid profile: resolver/sidecar failure is surfaced as a failed slice job; no Library output and no queue item are created.
- Timeout: HTTP timeout is surfaced as a failed slice job; temporary files are cleaned and no queue item is created.
- Rollback: remove WP-010 additive fixtures/tests/scripts and metadata additions. No database migration is planned.

# Risks and Human Gates

- Real Orca sidecar execution is currently blocked by a reproducible OrcaSlicer 2.3.2 SIGSEGV in every available Bambuddy sidecar image tag when loading the required printer+process profile pair.
- Because `make harness-orca-slice` cannot produce the `.gcode.3mf`, the Done When item "real Orca sidecar produces the expected artifact" is not satisfied.
- Commit, push, and PR creation are gated by all Done When conditions and therefore must not proceed until the sidecar blocker is resolved.
- No hardware test is in scope. Simulation or real sidecar success is not physical printer safety evidence.

# Outcomes

Blocked on real sidecar artifact generation. The Bambuddy server-side Library integration and deterministic harness/mock coverage are implemented, and sidecar health passes, but direct real Orca slicing fails before Bambuddy can complete the full WP-010 end-to-end path.

# Focused Orca SIGSEGV Diagnosis — 2026-06-24T13:20:00+09:00

Diagnosis only; WP-010 implementation was not continued. Artifacts are under ignored path
`harness/artifacts/orca-debug/run-20260624T131449/`.

## Reproduction Command

Existing harness reproduction remains:

```bash
make harness-up-slicer
make harness-orca-slice
```

Observed `make harness-orca-slice` failure:

```text
Orca slice failed with HTTP 500: {"message":"Failed to slice the model","details":"Slicer process failed (signal SIGSEGV)"}
```

Smallest reduced direct CLI reproducer, bypassing Bambuddy and the HTTP wrapper payload:

```bash
docker exec farm_harness-orca-slicer-api-1 \
  /app/squashfs-root/AppRun \
  --slice 1 \
  --load-settings '/app/squashfs-root/resources/profiles/BBL/machine/Bambu Lab P1P 0.4 nozzle.json;/app/squashfs-root/resources/profiles/BBL/process/0.20mm Standard @BBL P1P.json' \
  --allow-newer-file \
  --outputdir /tmp/wp010-orca-debug-run20260624T131449/out-raw-bundled-printer-process-gcode \
  /tmp/wp010-orca-debug-run20260624T131449/fixture-cube-v1.stl
```

That exits `139` with no stdout/stderr and no output artifact.

## Image, Digest, and Harness Wiring

Running sidecar image actually used:

```text
ghcr.io/maziggy/orca-slicer-api:bambuddy-0.2.4.7@sha256:c145e03b121d201dd0469b31758948e1932d2e6b4d8ddad92fe142a005919c80
image id/digest: sha256:c145e03b121d201dd0469b31758948e1932d2e6b4d8ddad92fe142a005919c80
```

Harness wiring verified from `docker inspect`:

```text
/app/data <- farm_harness_harness_orca_data (rw Docker volume)
3000/tcp -> 127.0.0.1:13003
ORCASLICER_PATH=/app/squashfs-root/AppRun
DATA_PATH=/app/data
```

No host fixture mount is involved in the sidecar. The HTTP wrapper uploads bytes into
sidecar temp files under `/tmp/slice-*`; direct CLI reproduction copied the same fixture
into `/tmp/wp010-orca-debug-run20260624T131449/`. This rules out harness host-volume path
wiring as the cause of the SIGSEGV.

## Exact HTTP Payload Evidence

The exact full WP-010-style HTTP request sent by the diagnostic run is retained as:

```text
harness/artifacts/orca-debug/run-20260624T131449/http-valid-stl-full.request.bin
sha256: 75b1728584c14a323a092f371793807b14008b89be13d6e426aba037a659d243
request bytes: 2255651
POST /slice
Content-Type: multipart/form-data; boundary=wp010-debug-valid-stl-full
```

Multipart fields:

```text
file=fixture-cube-v1.stl content_type=model/stl size=2230384 sha256=235784093ac09f0c0390309d8bc8092161a7100c11e1667334887be29f6c7c0e
printerProfile=p1p-printer.json content_type=application/json size=18750 sha256=201cb955124b68e1d1d56607f1604ee7dec3a80c2160d78bf4320a3a2f3e65b0
presetProfile=p1p-pla-process.json content_type=application/json size=3010 sha256=bb375965add0424df1809d0d2567663911e9ee539ad561d4a577c27e01f6933e
filamentProfile=p1p-pla-filament.json content_type=application/json size=2648 sha256=6d27969abbb56d4bd88160ccd6221df600011b360061d82993756a25bf48693e
exportType=3mf sha256=e859bfe69acbc1107c9181360bd395939fecdefb6651a656ddc3b3d37d94f902
```

HTTP response:

```text
harness/artifacts/orca-debug/run-20260624T131449/http-valid-stl-full.response.bin
HTTP 500: {"message":"Failed to slice the model","details":"Slicer process failed (signal SIGSEGV)"}
```

## Sidecar CLI Invocation

Live `execve` tracing was not available because the image does not include `strace`.
The CLI invocation is discoverable from `/app/dist/routes/slicing/slicing.service.js`:

```text
spawn(process.env.ORCASLICER_PATH, args, { env: process.env })
args = [
  --export-3mf result.3mf when exportType=3mf,
  --slice <plate>,
  --load-settings <resolved-printer.json>;<resolved-process.json> when both are present,
  --load-filaments <resolved-filament.json>[;...] when filaments are present,
  --allow-newer-file,
  --outputdir <workdir>/output,
  <workdir>/input/<uploaded-model>
]
```

The corrected direct CLI matrix used this same argument order.

## Direct CLI Matrix

From `harness/artifacts/orca-debug/run-20260624T131449/summary.json`:

```text
valid STL, no custom profiles: rc=205, safe validation failure, result.json written
valid STL, only printer profile: rc=239, safe compatibility failure, result.json written
valid STL, only process profile: rc=239, safe compatibility failure, result.json written
valid STL, printer+process: rc=139, SIGSEGV
valid STL, printer+process+filament: rc=139, SIGSEGV
valid STL, printer+process without --export-3mf: rc=139, SIGSEGV
valid STL, printer+process+filament without --export-3mf: rc=139, SIGSEGV
invalid STL, printer+process+filament: rc=250, safe model-parse failure, result.json written
existing bundled OrcaCube_v2.3mf, no custom profiles: rc=139, SIGSEGV
existing bundled OrcaCube_v2.3mf, no custom profiles, no --export-3mf: rc=139, SIGSEGV
raw copied P1P printer+process profiles, no --export-3mf: rc=139, SIGSEGV
raw bundled P1P printer+process profiles from image, no --export-3mf: rc=139, SIGSEGV
```

Smallest non-SIGSEGV direct CLI behavior:

```text
valid STL with no custom profiles -> rc=205 validation failure
invalid STL with full profile set -> rc=250 model parse failure
```

Smallest passing direct slice:

```text
None found in this image. Health passes, but every direct slice case that should produce an artifact either fails validation or SIGSEGVs.
```

## Profile Fixture Provenance

The profile fixtures are not handcrafted minimal JSON. Their hashes exactly match the
sidecar image's bundled BBL profiles:

```text
201cb955124b68e1d1d56607f1604ee7dec3a80c2160d78bf4320a3a2f3e65b0  BBL/machine/Bambu Lab P1P 0.4 nozzle.json
bb375965add0424df1809d0d2567663911e9ee539ad561d4a577c27e01f6933e  BBL/process/0.20mm Standard @BBL P1P.json
6d27969abbb56d4bd88160ccd6221df600011b360061d82993756a25bf48693e  BBL/filament/Generic PLA High Speed @BBL P1P.json
```

The sidecar resolver also produced flattened profiles, but raw bundled profiles reproduce
the same `rc=139` crash. Replacing handcrafted profile JSON is therefore not the next fix;
these fixtures are already real bundled Orca/Bambu profile files.

## Cause Classification

Classification: external dependency / sidecar image-Orca CLI blocker.

Evidence by requested category:

```text
A. sidecar image / Orca CLI: likely. OrcaSlicer 2.3.2 AppRun in the pinned image SIGSEGVs on direct CLI slice paths, including raw bundled BBL profiles and bundled OrcaCube_v2.3mf.
B. fixture STL: unlikely. The fixture STL without profiles fails safely, and invalid STL fails safely before slicing.
C. profile fixtures: unlikely as handcrafted/bad fixtures. They exactly match bundled BBL image profiles; raw bundled paths still SIGSEGV.
D. wrapper API payload: unlikely. Direct CLI bypassing HTTP reproduces rc=139.
E. CLI invocation arguments: the first WP-required crashing option is --load-settings with a semicolon-separated printer+process pair. The broader root is the image/CLI handling of that valid Orca invocation/profile pair, not --export-3mf or --load-filaments.
F. harness volume/path wiring: unlikely. Direct CLI uses container-local /tmp paths and still SIGSEGVs; /app/data volume and localhost port mapping are correct.
```

## Suspected Root Cause

The pinned `ghcr.io/maziggy/orca-slicer-api:bambuddy-0.2.4.7` image packages an OrcaSlicer
2.3.2 CLI/AppRun that is not stable for the required headless direct-slice mode in this
harness. The minimal WP-triggering crash is loading the real bundled BBL P1P printer and
process profiles through `--load-settings <printer>;<process>`. The bundled 3MF no-profile
SIGSEGV shows the image/CLI has a broader direct-slice crash path beyond the WP fixture STL.

## Next Recommended Fix

Do not weaken WP-010 Done When and do not remove real-sidecar artifact generation. Replace
or rebuild the pinned Orca sidecar image only after it passes an image-qualification smoke
inside the container:

```text
1. /app/squashfs-root/AppRun --slice 1 --outputdir <tmp> <bundled OrcaCube_v2.3mf> must produce an artifact or a non-crashing deterministic result.
2. /app/squashfs-root/AppRun --slice 1 --load-settings '<real exported or bundled printer>;<real exported or bundled process>' --load-filaments '<matching filament>' --outputdir <tmp> <fixture STL> must produce an artifact.
3. The sidecar HTTP /slice endpoint must then pass with the same profile bundle and retain exact request/response evidence.
```

If this sidecar codebase remains the chosen sidecar, open/fix the image/Orca packaging with
the reduced commands above, or pin a known-good OrcaSlicer CLI build that passes those
preflight checks. Do not switch WP-010 to Bambu Studio and do not proceed to PR until
`make harness-orca-slice` produces the required real `.gcode.3mf` artifact.

## Post-Diagnosis Validation

Fresh commands after the report update:

```text
make verify-fast: passed; 11 harness tests and 2 characterization tests passed, fast deterministic gate passed.
make harness-orca-health: passed; HTTP 200, OrcaSlicer 2.3.2 available, dataPath accessible.
make harness-orca-slice: still failing as expected for the blocker; HTTP 500 with details "Slicer process failed (signal SIGSEGV)".
```

No commit, push, PR, product implementation change, architecture switch, real printer connection,
production credential use, or WP-010 Done When weakening was performed during this diagnosis.

# Qualified Image Resume — 2026-06-24T14:51:14+09:00

## New Root Cause Classification

Classification: stale fixture / sidecar image contract mismatch.

Evidence:

```text
make harness-orca-health: passed; HTTP 200, OrcaSlicer 2.3.1 available.
make harness-orca-slice before fixture update: failed with HTTP 500 and Orca parameter validation, not SIGSEGV.
Direct CLI with current STL + qualified image bundled P1P profiles: exit 0, produced result.3mf and plate_1.gcode.
HTTP /slice with current STL + qualified image bundled P1P profiles: HTTP 200, returned 1,384,681 bytes.
```

The checked-in P1P printer/process/filament profile fixtures were copied from a different Orca image/profile set. Their hashes did not match the profiles bundled in the qualified `bbangjun360` image, and the qualified image rejected them during slicing.

## Fix Applied

- `.env.harness.example` now documents the qualified forked sidecar image:
  `ghcr.io/bbangjun360/orca-slicer-api:headless-cli-qualified-20260624@sha256:1c3e3e6f2ff193d4202bc7284af5d5a8c868e6185a26eeeacf878fe51387f518`.
- `harness/tests/test_orca_wp010_contract.py` now validates that `ORCA_IMAGE` is a GHCR image, digest-pinned with `@sha256:`, from an accepted repository, and not `latest` or an unpinned tag.
- `harness/fixtures/orca/profiles/p1p-printer.json`, `p1p-pla-process.json`, and `p1p-pla-filament.json` now match the qualified image's bundled BBL P1P profiles.
- `harness/fixtures/orca/profile-set.json` now records the updated profile hashes and profile-set hash.

Updated profile hashes:

```text
aa99c3b54c3b101c614185b67dc0049086d95ae5caa13d5aae29c2f5675caade  p1p-printer.json
eb93e7d484b07b2a4b104786d3bee4b9abb02cd027245106b3936ee1671a7e48  p1p-pla-process.json
df86ae82538383f80f1592de687a1f50b4f969d8797d12f582acb93f15271316  p1p-pla-filament.json
bc84ff64720c274b6ed7c959e3ac28a4c2aff03caceb08f6ffb0e7250784627c  profile_set_sha256
```

## Latest Validation

```text
make harness-orca-health: passed; HTTP 200, OrcaSlicer 2.3.1.
make harness-orca-slice: passed; wrote harness/artifacts/orca/fixture-cube-v1.gcode.3mf, 1,384,681 bytes, output SHA-256 5f417f0f7103ac9605f91e6f0462851cc67e81b25b7088b5acdf129eb54d7f5b.
make verify-fast: passed; 14 harness tests and 2 characterization tests.
make test-contract: passed; 14 harness tests.
make test-unit: passed; backend py_compile and 14 harness tests.
make test-integration: passed; Bambuddy root, /health, /docs, and mock-services health returned 200.
docker run --rm -v /home/fai/workspace/bambuddy:/workspace -w /workspace farm_harness-bambuddy:latest python -m pytest -p no:cacheprovider backend/tests/integration/test_library_slice_api.py: passed; 39 tests.
```

The first local attempt to run `python3 -m unittest backend.tests.integration.test_library_slice_api` failed because the host Python environment lacks `httpx`; the direct container `python -m unittest` form found zero tests because the file is pytest-style. The bind-mounted Docker pytest command above is the successful targeted backend verification.

No printer dispatch was introduced. The WP-010 backend pytest coverage includes no-queue assertions, and the Orca compose contract still rejects Bambu credential fields and host/printer VLAN networking. No Bambu printer credentials were passed to Orca.

## Current Outcome

WP-010 real sidecar artifact generation is unblocked with the qualified forked image. Remaining work is staging only reviewed non-secret paths, then commit/push/PR only after the staged set excludes `.env.harness`, generated artifacts, logs, temp files, secrets, and ignored files.
